# Backend API origin. Leave empty for local dev (Vite proxies /api and
# /socket.io to localhost:3001 — see vite.config.js). In production, set
# this to your deployed backend's origin, e.g. https://metamaths-api.onrender.com
VITE_API_URL=
